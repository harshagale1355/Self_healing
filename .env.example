# Copy to `.env`. If you only set GROQ_API_KEY, the app uses Groq for all LLM calls automatically.
GROQ_API_KEY=
# Optional:
# GROQ_MODEL=llama-3.3-70b-versatile
# LLM_PROVIDER=openai   # only if you need OpenAI despite having both keys

# OpenAI instead/alongside:
# OPENAI_API_KEY=
